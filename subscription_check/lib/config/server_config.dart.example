// 실제 백엔드 연결은 dart-define 으로 켭니다.
// 기본 실행은 BACKEND_ENABLED=false 이며 mock 데이터만 사용합니다.

// 예:
// flutter run -d chrome --dart-define=BACKEND_ENABLED=true
// flutter run --dart-define=BACKEND_ENABLED=true --dart-define=SERVER_BASE_URL=https://example.com
